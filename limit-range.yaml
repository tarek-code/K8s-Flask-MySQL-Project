apiVersion: v1
kind: LimitRange
metadata:
  name: default-limits
spec:
  limits:
    - default:
        memory: 1Gi
        cpu: 1
      defaultRequest:
        memory: 512Mi
        cpu: 250m
      type: Container

